import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  height: 4rem;
  margin: 4rem 0;

  display: flex;
  justify-content: center;

  img {
    cursor: pointer;
  }
`;
