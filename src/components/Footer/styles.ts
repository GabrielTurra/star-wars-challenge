import styled from 'styled-components';

export const Container = styled.div`
  padding: 3rem;
  text-align: center;
  display: flex;
  justify-content: center;
  font-size: 0.75rem;
  color: var(--gray-300);

  a {
    margin-left: 3rem;
  }
`;
